# HumanMark Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Server Configuration
# =============================================================================

# Server port (default: 8080)
PORT=8080

# Environment: development, staging, production (default: development)
ENV=development

# Logging level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL connection string (optional - uses in-memory storage if not set)
# Format: postgres://user:password@host:port/database?sslmode=disable
# DATABASE_URL=postgres://humanmark:password@localhost:5432/humanmark?sslmode=disable

# Redis connection string (optional - for caching and rate limiting)
# Format: redis://user:password@host:port/db
# REDIS_URL=redis://localhost:6379/0

# =============================================================================
# Detection API Keys
# =============================================================================
# At least one detection API is required for production use.
# Without any API keys, a mock detector is used (development only).

# Hive AI API key - supports all content types
# Get your key at: https://thehive.ai
# HIVE_API_KEY=your-hive-api-key-here

# OpenAI API key - supports text detection
# Get your key at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-openai-api-key-here

# GPTZero API key - supports text detection
# Get your key at: https://gptzero.me/api
# GPTZERO_API_KEY=your-gptzero-api-key-here

# =============================================================================
# Rate Limiting
# =============================================================================

# Maximum requests per minute per IP/API key (default: 60)
RATE_LIMIT_PER_MINUTE=60

# =============================================================================
# File Uploads
# =============================================================================

# Maximum upload size in bytes (default: 104857600 = 100MB)
MAX_UPLOAD_SIZE=104857600

# =============================================================================
# Security
# =============================================================================

# CORS allowed origins (comma-separated, default: * in development)
# In production, set this to your actual domains
# ALLOWED_ORIGINS=https://example.com,https://app.example.com

# Require API key for all requests (default: false in dev, true in production)
# API_KEY_REQUIRED=false
